package ru.mtuci.sportapp.backend.entity;

// TrainingGroup — перечисление групп, совпадает с тем, что будет на фронте
public enum TrainingGroup {
    JUNIORS,   // юниоры
    SENIORS    // старшие
}
